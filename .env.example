# Plantilla de variables de entorno para PawSpace.
# Copia este archivo a .env y ajusta los valores antes de levantar docker-compose.

# Database
DB_ROOT_PASSWORD=rootpass
DB_NAME=pawspace
DB_USER=pawuser
DB_PASSWORD=pawpass

# Backend
DB_HOST=localhost
DB_PORT=3306
